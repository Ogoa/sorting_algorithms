This directory contains files created under the `0x0B. C - Sorting algorithms & Big O` project.<br>

To clone this repo, use the command:
```
$ git clone git@github.com:Ogoa/sorting_algorithms.git
```
